name: Publish Python

# Trigger: tag push v* — publishes theveil to PyPI via OIDC trusted publisher
#
# Placeholder only. Wired up in the SDK publishing arc.
#
# Intended structure (commented skeleton):
#
# on:
#   push:
#     tags:
#       - 'v*'
#
# jobs:
#   publish:
#     runs-on: ubuntu-latest
#     permissions:
#       id-token: write       # required for PyPI trusted publishing (OIDC)
#       contents: read
#     defaults:
#       run:
#         working-directory: python
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-python@v5
#         with:
#           python-version: '3.12'
#       - name: Build
#         run: |
#           python -m pip install --upgrade build
#           python -m build
#       - name: Publish to PyPI
#         uses: pypa/gh-action-pypi-publish@release/v1
#         with:
#           packages-dir: python/dist

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "publish-python placeholder — wired up in SDK publishing arc"
